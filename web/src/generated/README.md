# Generated API contract

Do not edit `openapi.json` or `api-types.ts` directly. Regenerate both from the
FastAPI application contract with:

```text
npm --prefix web run contracts:generate
```

FastAPI OpenAPI remains authoritative; these files are checked-in derivative
artifacts for deterministic frontend builds and freshness checks.
