import { StrategyToRiskWorkspace } from "@/components/strategy-to-risk-workspace";
import { WorkspaceShell } from "@/components/workspace-shell";

export default function StrategyToRiskPage() {
  return (
    <WorkspaceShell>
      <StrategyToRiskWorkspace />
    </WorkspaceShell>
  );
}
